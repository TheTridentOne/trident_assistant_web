<%= render "collections/header", collection: @collection %>

<div class="flex items-center mb-6 space-x-4">
  <%= link_to 'Items', collection_items_path(@collection, type: :drafted), class: 'underline' %>
  <div class="">/</div>
  <div>Edit #<%= @item.identifier %></div>
</div>

<div class="max-w-screen-md mx-auto">
  <%= render "collections/items/form", item: @item %>
</div>
