<%= render "collections/header", collection: @collection %>

<div class="flex items-center mb-6 space-x-4">
  <%= link_to 'Items', items_path(collection_id: @collection.id), class: 'underline' %>
  <div class="">/</div>
  <div>New NFT</div>
</div>

<div class="max-w-screen-md mx-auto">
  <%= render "collections/generate_nft_tasks/form", task: @collection.tasks.new(type: 'GenerateNftTask') %>
</div>
