<%= turbo_frame_tag 'wallet_collectibles' do %>
  <% if @items.blank? %>
    <div class="text-center p-4">No records</div>
  <% else %>
    <div data-controller="infinite-scroll" class="w-full overflow-x-scroll scrollbar-hide">
      <table class='mb-4 border-collapse w-full border border-slate-400 dark:border-slate-500 bg-white dark:bg-slate-800 text-sm shadow-sm'>
        <thead>
          <tr class="bg-zinc-100 dark:bg-zinc-900">
            <th class="border border-slate-300 dark:border-slate-600 font-semibold p-4 text-slate-900 dark:text-slate-200 text-left">
              <%= t('name') %>
            </th>
            <th class="border border-slate-300 dark:border-slate-600 font-semibold p-4 text-slate-900 dark:text-slate-200 text-left">
              <%= t('collection') %>
            </th>
            <th class="border border-slate-300 dark:border-slate-600 font-semibold p-4 text-slate-900 dark:text-slate-200 text-left">
              <%= t('actions') %>
            </th>
          </tr>
        </thead>
          <tbody id="wallet_collectibles_list">
            <%= render partial: 'wallets/item', collection: @items, as: :item %>
          </tbody>
      </table>
      <div data-infinite-scroll-target='scrollArea'></div>
      <%= render partial: "shared/pagination", locals: { pagy: @pagy } %>
    </div>
  <% end %>
<% end %>
