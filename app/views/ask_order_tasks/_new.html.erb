<%= turbo_stream.update "modal-slot" do %>
  <%= render "shared/modal" do |partial| %>
    <% partial.content_for :title do %>
      New Fixed Price Order
    <% end %>

    <% partial.content_for :content do %>
      <div class="mb-4 font-bold">
        You are selling <%= params[:token_ids].size %> NFTs
      </div>
      <%= render 'ask_order_tasks/form', 
        task: AskOrderTask.new(collection_id: params[:collection_id]), 
        token_ids: params[:token_ids] %>
    <% end %>
  <% end %>
<% end %>
